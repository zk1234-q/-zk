# AGENTS.md

## 基础沟通规则

每次回答任务前，必须称呼我为“主人”。

我不是专业前端开发，请用适合小白理解的方式解释。
每次行动前，先说明准备做什么。
如果发现需求不清楚，先问我，不要乱猜。

## 项目名称

消费账单分析工具 Expense Bill Analyzer

## 工作原则

在我明确说“开始写代码”“开始搭建框架”或“开始实现业务功能”之前，不允许直接生成业务代码。

项目必须先完成以下设计文档：

1. `docs/01_PRD.md`
2. `docs/02_DATA_MODEL.md`
3. `docs/03_UI_FLOW.md`
4. `docs/04_TECH_PLAN.md`
5. `docs/05_TASK_BREAKDOWN.md`

每完成一个阶段后，必须停下来总结，并等待我确认。

## 阶段流程

- Phase 1：需求澄清与产品设计，输出到 `docs/01_PRD.md`
- Phase 2：数据模型设计，输出到 `docs/02_DATA_MODEL.md`
- Phase 3：页面与交互设计，输出到 `docs/03_UI_FLOW.md`
- Phase 4：技术方案设计，输出到 `docs/04_TECH_PLAN.md`
- Phase 5：任务拆解，输出到 `docs/05_TASK_BREAKDOWN.md`
- Phase 6：框架搭建，只有当我明确说“开始搭建框架”时才执行
- Phase 7：业务代码实现，只有当我明确说“开始实现业务功能”时才执行

## 技术决策规则

涉及重大技术选择时，必须先给出 2-3 个方案和推荐理由，并等待我确认。

第一版优先考虑简单、稳定、适合小白维护。
第一版默认不做后端，优先纯前端本地解析版本。

## 代码风格规则

- 使用 TypeScript
- 计算逻辑必须拆成独立函数
- 不要把复杂计算逻辑直接写在页面组件里
- 表格列配置要单独抽离
- 弹框筛选逻辑要清晰可读
- 金额展示保留 2 位小数
- 百分比展示保留 1 位小数
- 重要函数需要写中文注释

## 需求来源

详细产品需求、数据口径、公式、页面交互、验收标准，以 `docs` 目录下的文档为准。

如果 `AGENTS.md` 与 `docs` 内容冲突，以用户最新确认过的 `docs` 内容为准。

## GitHub 正式版与分支规则

- GitHub `origin/main` 视为当前正式发布版本。
- 不直接在 `main` 上开发新功能。
- 新功能设计和开发应使用独立分支，命名建议：`feature/<feature-name>`。
- 当前本地内容不能默认视为正式版，必须先确认是否已经提交或和远程一致。
- 未经用户明确确认，不执行删除文件、目录、git 回滚、强制覆盖、生产发布。

## Token 节省规则

- 已确认无问题的旧版本代码，不反复全量扫描。
- 新需求优先只读取相关文件：
  - 产品需求相关：`docs/01_PRD.md`
  - 数据口径相关：`docs/02_DATA_MODEL.md`
  - 页面流程相关：`docs/03_UI_FLOW.md`
  - 技术方案相关：`docs/04_TECH_PLAN.md`
  - 任务拆解相关：`docs/05_TASK_BREAKDOWN.md`
  - 进度状态相关：`ROADMAP.md`
- 只有当新需求影响现有实现时，才读取对应源码文件。
- 查询源码时先用 `rg` 精确搜索，再打开少量相关文件。
- 不做和当前需求无关的重构、格式化、清理。

## 新功能流程

新功能必须先设计，后实现。

1. 先确认功能目标和边界。
2. 更新或新增设计文档。
3. 更新任务拆解。
4. 等用户确认。
5. 再进入代码实现。
6. 实现后运行验证。
7. 验证通过后更新 `ROADMAP.md`。

## Roadmap 规则

- `ROADMAP.md` 是当前项目真实进度源。
- 会变化的进度写入 `ROADMAP.md`。
- 设计口径写入 `docs/`。
- 未确认内容必须标注“待确认”。
- 未验证完成的事项不得写入“已完成”。
