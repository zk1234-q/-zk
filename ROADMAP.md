# ROADMAP.md

## 当前状态

- 项目：消费账单分析工具 Expense Bill Analyzer
- 第一正式版本来源：GitHub `origin/main`
- 本地应用版本：`expense-bill-analyzer@0.1.0`
- 本地测试地址：`http://127.0.0.1:5173/-zk/`
- 本地测试启动入口：项目根目录 `start-local-test.bat`
- 本地 Git 状态：当前 `main` 无本地提交，`expense-bill-analyzer/` 和 `素材/` 为未跟踪内容
- 远程 `origin/main` 最新提交：`96237339b7f319f8deba71ab23a4096a2e471ecd`
- 当前阶段：V2.6 目标管理已实现并构建通过，等待用户验收

## 已完成

- 已确认 GitHub 远程仓库连接：`https://github.com/zk1234-q/-zk.git`
- 已确认 GitHub `origin/main` 是用户认可的第一正式版本
- 已将后续工作规则合并到项目内 `AGENTS.md`
- 已建立项目内 `ROADMAP.md`
- 已确认后续工作在第一版本基础上进行第二版本设计和开发
- 已根据用户确认的需求补充第二版 PRD：独立手工预算管理，不和账单分析表联动
- 已根据用户新增需求调整第二版范围：默认月收入设置、账单大类预算、手工购物预算管理、重复月份上传覆盖确认优化
- 已根据用户确认的纯本地迁移方案，补充设置页导出 / 导入本地数据文件能力
- 已补充账单大类预算联动规则：按月份和一级分类名称匹配账单支出，超预算时在一级分类金额上标红并展示预算提示
- 已补充账单大类预算增强：复制月份预算、从已导入账单一级分类中选择大类、月度预算总览、超支备注、预算使用率颜色、月度结算视图
- 已确认“未设置预算的大类提醒”第二版暂不优先做
- 已明确云端保存、账号登录、跨设备同步放入第三版规划
- 已补充第二版数据模型：用户设置、总预算设置、月度大类预算、购物预算、导出导入备份结构、账单摘要字段扩展
- 已补充第二版页面与交互设计：设置页、预算管理页、月度预算总览、月度结算视图、独立购物预算区、覆盖确认弹窗、导入确认弹窗、月度分析预算联动
- 已补充第二版技术方案：新增页面组件、类型、计算函数、IndexedDB store、覆盖确认、预算联动、导出导入、兼容迁移和验证方案
- 已补充第二版任务拆解：实现准备、计算逻辑、设置页、预算管理页、账单保存优化、月度分析联动、整体验证
- 已创建第二版开发分支：`feature/v2-local-budget-management`
- 已实现第二版类型、IndexedDB 扩展、设置存储、预算存储、备份导入导出、预算计算函数
- 已实现设置页：默认月收入、本地数据导出、本地数据导入
- 已实现预算管理页：总预算设置、月度大类预算、复制月份预算、月度预算总览、月度结算、独立购物预算
- 已实现重复月份账单覆盖确认弹窗，并补充账单摘要字段
- 已实现月度分析预算联动：默认月收入读取设置、一级分类预算状态、明细弹框预算信息
- 已新增本地测试一键启动入口：项目根目录 `start-local-test.bat`，用于固定启动并打开 `http://127.0.0.1:5173/-zk/`
- 已新增 `AI_CONTEXT.md`，用于后续新会话低 token 读取项目短上下文
- 已新增模块化文档结构：`docs/modules/01_BILL_MANAGER.md`、`02_ANALYSIS.md`、`03_BUDGET.md`、`04_ASSET.md`、`05_GOAL.md`、`06_SETTINGS.md`
- 已新增 V2.1 架构治理文档：`docs/00_PROJECT_CONTEXT.md`、`docs/90_DATA_MODEL.md`、`docs/91_UI_NAVIGATION.md`、`docs/92_CLOUD_READY_ARCHITECTURE.md`、`docs/99_TASK_BREAKDOWN.md`
- 已确认后续版本顺序：V2.1 架构治理与验收修复、V2.5 资产总览、V2.6 目标管理、V3.0 云端版
- 已实现 V2.1 repository 数据访问层：页面通过 `src/repositories/*` 访问数据，本地 IndexedDB 实现在 `src/repositories/local/*`
- 已将系统名称改为“账单管理分析系统”，副标题改为“账单分析 · 资产盘点 · 目标管理”
- 已精简左侧导航：`上传账单` 和 `数据预览` 改为账单管理流程页，不再作为常驻菜单
- 已优化月度汇总表：按月份倒序，支持年份筛选，默认最新年份，每页 12 条
- 已优化品牌区图标、顶部标题布局和月度汇总筛选工具条样式
- 已实现 V2.5 资产总览本地版：资产负债档案、档案启用/停用、资产记录新增、历史记录、编辑、复制新增、删除
- 已实现资产记录自动计算：总资产、总负债、净资产
- 已扩展 IndexedDB 到版本 3，新增 `assetAccounts` 和 `assetSnapshots` store
- 已扩展本地备份到 schemaVersion 3，导出/导入包含资产档案和资产记录，并兼容 schemaVersion 2 旧备份
- 已实现 V2.6 目标管理本地版：目标档案新增和编辑、目标金额、已完成金额、目标日期、状态、备注
- 已实现目标自动计算：剩余金额、完成率、剩余月份、每月需存金额
- 已扩展 IndexedDB 到版本 4，新增 `goals` store
- 已扩展本地备份到 schemaVersion 4，导出/导入包含目标数据，并兼容 schemaVersion 2 和 3 旧备份

## 进行中

- 等待用户验收 V2.6 目标管理

## 待办

- V3.0：云端版设计，包含账号、后端、数据库、本地数据迁移到云端

## 阻塞

- 暂无

## 最近验证

- 2026-06-26：已执行 `npm run build`，构建通过
- 2026-06-26：已读取远程 `origin/main`，远程最新提交为 `96237339b7f319f8deba71ab23a4096a2e471ecd`
- 2026-06-26：已再次执行 `npm run build`，第二版本地增强版构建通过
- 2026-06-26：已启动 Vite dev server 并请求 `http://127.0.0.1:5173`，返回 HTTP 200
- 2026-06-29：已执行 `start-local-test.ps1 -NoBrowser`，识别本地测试服务正常运行；请求 `http://127.0.0.1:5173/-zk/` 返回 HTTP 200
- 2026-06-29：已执行 `npm run build`，构建通过；Vite 仍提示单个 JS chunk 超过 500 kB，为体积提醒，不影响本次启动脚本
- 2026-06-29：已完成 V2.1 模块化文档治理；本次为文档新增，未运行前端构建
- 2026-06-29：已执行 `npm run build`，V2.1 架构治理和验收修复构建通过；Vite 仍提示单个 JS chunk 超过 500 kB
- 2026-06-29：已启动 Vite 本地服务并请求 `http://127.0.0.1:5173/-zk/`，返回 HTTP 200
- 2026-06-29：已执行 `npm run build`，V2.5 资产总览构建通过；Vite 仍提示单个 JS chunk 超过 500 kB
- 2026-06-29：已启动 Vite 本地服务并请求 `http://127.0.0.1:5173/-zk/`，返回 HTTP 200
- 2026-06-29：已执行 `npm run build`，V2.6 目标管理构建通过；Vite 仍提示单个 JS chunk 超过 500 kB
- 2026-06-29：已启动 Vite 本地服务并请求 `http://127.0.0.1:5173/-zk/`，返回 HTTP 200

## 后续工作原则

- 不主动修改旧版本代码
- 新会话优先读取 `AI_CONTEXT.md`
- 新需求按模块读取 `docs/modules/` 中的对应文档和必要源码
- 不重复全量理解项目
- 所有实现完成并验证后，必须同步更新本文件
